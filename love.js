$(document).ready(function () {
  // Function to simulate interactions with the page
  function simulateInteraction() {
    // Customize your interactions here
    // For example, you can trigger a click on an element
    $(".ico").click();
  }

  // Set an interval to simulate interactions every 1000 milliseconds (1 second)
  setInterval(simulateInteraction, 1000);
});
